platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.9

params:
  ARCH: unset

inputs:
  - name: repo
  - name: version

outputs:
  - name: output

run:
  path: repo/build/scripts/build-docdepot.sh
